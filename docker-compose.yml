version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: my-pwa-app
    ports:
      - "8080:80"
    restart: unless-stopped
    networks:
      - pwa-network

networks:
  pwa-network:
    driver: bridge